/// --- Set up a system ---
//////////////////////////
//////////////////////////class RotatorSystem {
//////////////////////////  // this group will contain every entity that has a Transform component
//////////////////////////  group = engine.getComponentGroup(Transform)
//////////////////////////
//////////////////////////  update(dt: number) {
//////////////////////////    // iterate over the entities of the group
//////////////////////////    for (let entity of this.group.entities) {
//////////////////////////      // get the Transform component of the entity
//////////////////////////      const transform = entity.getComponent(Transform)
//////////////////////////
//////////////////////////      // mutate the rotation
//////////////////////////      transform.rotate(Vector3.Up(), dt * 10)
//////////////////////////    }
//////////////////////////  }
//////////////////////////}
//////////////////////////
//////////////////////////// Add a new instance of the system to the engine
//////////////////////////engine.addSystem(new RotatorSystem())
//////////////////////////
///////////////////////////// --- Spawner function ---
//////////////////////////
//////////////////////////function spawnCoin(x: number, y: number, z: number) {
//////////////////////////  // create the entity
//////////////////////////  const coin = new Entity()
//////////////////////////
//////////////////////////  // add a transform to the entity
//////////////////////////  coin.addComponent(new Transform({ position: new Vector3(x, y, z) }))
//////////////////////////
//////////////////////////  // add a shape to the entity
//////////////////////////  coin.addComponent(new BoxShape())
//////////////////////////
//////////////////////////  // add the entity to the engine
//////////////////////////  engine.addEntity(coin)
//////////////////////////
//////////////////////////  return coin
//////////////////////////}
//////////////////////////
///////////////////////////// --- Spawn a coin ---
//////////////////////////
//////////////////////////const coin = spawnCoin(8, 1, 8)
//////////////////////////
//////////////////////////coin.addComponent(
//////////////////////////  new OnClick(() => {
//////////////////////////    coin.getComponent(Transform).scale.z *= 1.1
//////////////////////////    coin.getComponent(Transform).scale.x *= 0.9
//////////////////////////
//////////////////////////    spawnCoin(Math.random() * 8 + 1, Math.random() * 8, Math.random() * 8 + 1)
//////////////////////////  })
//////////////////////////)
//////////////////////////